mkdir ws-server
cd ws-server
touch index.js          // 이 파일을 작성한다.
npm init --yes          // package.json 생성
npm install ws --save   // node_modules 생성
touch server.js         // 이 파일을 작성한다.
node server.js          // 서버 실행.